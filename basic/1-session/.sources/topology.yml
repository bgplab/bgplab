# This topology is used to create the BGP configuration on X1

defaults.device: eos

module: [ bgp ]

nodes:
  rtr:
    bgp.as: 65000
  x1:
    device: cumulus
    id: 10
    bgp.as: 65100

links:
- rtr-x1
